# 工具环境

<!-- 在此记录特定环境下的工具配置和使用说明。
     例如：服务器地址、常用命令别名等。 -->

## 工具别名

（可通过 tools.toon 配置命令别名，例如：
  aliases = [
      { name = "deploy", command = "/opt/scripts/deploy.sh" }
  ]
）

## 环境说明

（在此记录当前部署环境的特殊配置。）
